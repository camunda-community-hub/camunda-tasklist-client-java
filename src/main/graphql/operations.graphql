
mutation UnclaimTask($id: String!) {
	unclaimTask(taskId: $id) {
		assignee
	}
}

mutation ClaimTask($id: String!, $assignee: String!, $allowOverrideAssignment: Boolean) {
	claimTask(taskId: $id, assignee: $assignee, allowOverrideAssignment:  $allowOverrideAssignment) {
		id
		assignee
	}
}

mutation CompleteTask($id: String!, $variables: [VariableInput!]!) {
	completeTask(taskId: $id, variables: $variables) {
		id
		taskState
		variables {
			name
			value
		}
		completionTime
	}
}

query GetTasks($candidateGroup: String, $assignee: String, $assigned: Boolean, $state: TaskState, $processDefinitionId: String, $processInstanceId: String, $taskDefinitionId: String, $pageSize: Int, $searchAfter: [String!], $searchBefore: [String!], $searchAfterOrEqual: [String!]) {
	tasks(query: {candidateGroup: $candidateGroup, assignee: $assignee, assigned: $assigned, state: $state, processDefinitionId: $processDefinitionId, processInstanceId: $processInstanceId, taskDefinitionId: $taskDefinitionId, pageSize: $pageSize, searchAfter: $searchAfter, searchBefore: $searchBefore, searchAfterOrEqual: $searchAfterOrEqual}) {
		id
		name
		processName
		assignee
		creationTime
		taskState
		candidateGroups
		sortValues
		isFirst
		formKey
		processDefinitionId
		taskDefinitionId
	}
}

query GetTasksWithVariable($candidateGroup: String, $assignee: String, $assigned: Boolean, $state: TaskState, $processDefinitionId: String, $processInstanceId: String, $taskDefinitionId: String, $pageSize: Int, $searchAfter: [String!], $searchBefore: [String!], $searchAfterOrEqual: [String!]) {
	tasks(query: {candidateGroup: $candidateGroup, assignee: $assignee, assigned: $assigned, state: $state, processDefinitionId: $processDefinitionId, processInstanceId: $processInstanceId, taskDefinitionId: $taskDefinitionId, pageSize: $pageSize, searchAfter: $searchAfter, searchBefore: $searchBefore, searchAfterOrEqual: $searchAfterOrEqual}) {
		id
		name
		processName
		assignee
		creationTime
		taskState
		candidateGroups
		sortValues
		isFirst
		formKey
		processDefinitionId
		taskDefinitionId
		variables {
			id
			name
			value
		}
	}
}

query GetTask($id: String!) {
  task(id: $id) {
    id
    formKey
    processDefinitionId
    assignee
    name
    taskState
    candidateGroups
    processName
    creationTime
    completionTime
    taskDefinitionId
  }
}

query GetTaskWithVariables($id: String!) {
  task(id: $id) {
    id
    formKey
    processDefinitionId
    assignee
    name
    taskState
    candidateGroups
    processName
    creationTime
    completionTime
    taskDefinitionId
    variables {
      id
      name
      value
    }
  }
}

query GetForm ($id: String!, $processDefinitionId: String!) {
    form (id: $id, processDefinitionId: $processDefinitionId) {
        id
        processDefinitionId
        schema
    }
}